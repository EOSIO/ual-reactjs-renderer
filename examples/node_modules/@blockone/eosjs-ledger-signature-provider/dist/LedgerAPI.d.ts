export interface LedgerAPIInterface {
    transport: any;
    addressIndex: number;
    scrambleKey: string;
}
/**
 * Ledger API
 * @param transport Ledger transport method
 */
export declare class LedgerAPI implements LedgerAPIInterface {
    transport: any;
    addressIndex: number;
    scrambleKey: string;
    constructor(transport: any);
    /**
     * @returns [keys] An array of public keys
     */
    getPublicKey(requestPermission?: boolean): Promise<string>;
    /**
     * @returns A Signed eos transaction
     */
    signTransaction({ chainId, serializedTransaction }: {
        chainId: string;
        serializedTransaction: Uint8Array;
    }): Promise<string[]>;
}
